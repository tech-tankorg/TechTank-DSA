class minStack {
  /*
    Design a stack that supports push, pop, top, and retrieving the minimum element in constant time.
    The minStack class should implement the following methods: push, pop, top, and getMin


    
    */

  constructor(initVars) {
    // Inilizses a stack object
  }

  push() {
    // This method is responsible for pushing an element onto a stack
  }
  pop() {
    // This method removes an element from the top on a stack
  }
  top() {
    // This method gets the top element of the stack
  }
  getMin() {
    // This method retrieves the minimum element of the stack
  }
}
